export type History = {
  title: string;
  date: Date;
  purpose: string;
  items: string;
  designRequest: string;
  generatedCode: string;
  generatedCSS: string;
};

export type HistoryList = History[];
